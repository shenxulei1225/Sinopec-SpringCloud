# 任务目录（底座 `task`）

巡检任务是子数据类型 `task_patrol`，步骤树开在**底座任务**上，物理表 `ent_task*`。

## 01 打开步骤树

混挂：检查项 + 动作。存盘形态 `step_nodes`。

## 02 金桥可见样例任务

站场级目录按所属场站过滤。写一条「金桥储罐区步骤树验收」，挂机器人头、一条检查项、机器人尾。

```bash
./import.sh
```

幂等。不生成头尾、不按路径串检查项。
