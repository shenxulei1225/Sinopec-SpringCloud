# 本地微服务怎么开

按职责开，不要记一串旧名。

| 你要干什么 | 命令 |
|---|---|
| 能登录、请求进得了门 | `./start-microservices.sh base` |
| 开数据页台账（类型/型号/实体） | 先 `base`，再 `./start-microservices.sh biz` |
| 开整份中台 | `./start-microservices.sh bmp` |
| 今天全部用到的都开 | `./start-microservices.sh all` |
| 只重启组件配置（改配置表） | `./start-microservices.sh stop resource && ./start-microservices.sh resource` |

`all` = 底盘 + 工作流 + 动态业务 + 中台，已经含 `bmp`，不用再开一遍。

Windows 把 `.sh` 换成 `.\start-microservices.ps1`。

旧名 `platform`、`platform-runtime` 仍能敲，会收到正名 `resource`、`runtime`。
