<configuration>
    <!-- 参考 org/springframework/boot/logging/logback/defaults.xml 配置,优化 CONSOLE_LOG_PATTERN、FILE_LOG_PATTERN -->
    <!-- 格式化输出：%d 表示日期,%thread 表示线程名,%-5level：级别从左显示 5 个字符宽度,%msg：日志消息,%n是换行符 -->
    <!-- CONSOLE_LOG_PATTERN 相比 FILE_LOG_PATTERN 多了 highlight、cyan 等高亮 -->
    <!-- 在日志格式中添加应用名称,使用 ${spring.application.name} 变量 -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="unknown"/>
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%magenta(${APP_NAME})] %highlight(%-5level) %cyan(%logger{50}:%L) - %msg%n"/>
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [${APP_NAME}] %-5level %logger{50}:%L - %msg%n"/>

    <!-- 控制台 Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">　　　　　
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 文件 Appender -->
    <!-- 参考 Spring Boot 的 file-appender.xml 编写 -->
    <appender name="FILE"  class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <!-- 日志文件名 -->
        <file>${LOG_FILE}</file>
        <!-- 滚动策略：基于【每天 + 大小】创建日志文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern> <!-- 日志文件输出的文件名 -->
            <maxHistory>30</maxHistory> <!-- 日志文件的保留天数 -->
            <maxFileSize>10MB</maxFileSize> <!-- 日志文件,到达多少容量,进行滚动 -->
        </rollingPolicy>
    </appender>
    <!-- 异步写入日志,提升性能 -->
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold> <!-- 不丢失日志。默认的,如果队列的 80% 已满,则会丢弃 TRACT、DEBUG、INFO 级别的日志 -->
        <queueSize>512</queueSize> <!-- 更改默认的队列的深度,该值会影响性能。默认值为 256 -->
        <appender-ref ref="FILE"/>
    </appender>

    <!-- SkyWalking Appender：GRPC 日志收集,实现日志中心 -->
    <!--
    <appender name="SKYWALKING" class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.log.GRPCLogClientAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                <pattern>[%tid] ${FILE_LOG_PATTERN}</pattern>
            </layout>
        </encoder>
    </appender>
    -->

    <!-- 过滤第三方库的 DEBUG 日志,只保留 WARN 及以上级别 -->
    <!-- 注意：日志中显示的包名可能是缩短形式（如 c.a.n.s...）,但实际配置需要使用完整包名或前缀 -->
    <!-- gRPC 相关日志设置为 WARN,避免大量 DEBUG 日志 -->
    <logger name="io.grpc" level="WARN"/>
    <!-- Nacos SDK 相关的 gRPC 日志（Nacos SDK 内部使用的 gRPC 客户端） -->
    <logger name="com.alibaba.nacos.sdk" level="WARN"/>
    <!-- Nacos 客户端相关日志设置为 WARN -->
    <logger name="com.alibaba.nacos" level="WARN"/>
    <logger name="com.alibaba.nacos.client" level="WARN"/>
    <logger name="com.alibaba.nacos.client.naming" level="WARN"/>
    <!-- Netty 相关日志设置为 WARN（gRPC 底层使用 Netty） -->
    <logger name="io.netty" level="WARN"/>
    
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <!-- 本地环境下,如果不想【FILE】打印日志,可以注释掉本行 -->
        <appender-ref ref="ASYNC"/>
        <!-- 如果想接入【SkyWalking 日志服务】,可以取消注释掉本行 -->
        <!-- <appender-ref ref="SKYWALKING"/> -->
    </root>

</configuration>
