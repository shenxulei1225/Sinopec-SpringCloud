<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.emergency.dal.mysql.plan.EmergencyPlanMapper">

    <!-- 根据响应级别推荐应急预案 -->
    <select id="selectRecommendedPlans" resultType="cn.iocoder.yudao.module.emergency.dal.dataobject.plan.EmergencyPlanDO">
        SELECT *
        FROM emergency_plan
        WHERE deleted = false
          AND status = 'published'
          <if test="planType != null">
              AND plan_type = #{planType}
          </if>
          AND plan_levels IS NOT NULL
          AND jsonb_array_length(plan_levels) > 0
          AND plan_levels @> #{responseLevel}::jsonb
        ORDER BY id DESC
    </select>

</mapper>

